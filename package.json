{
  "name": "fearngreed-flow-lab-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check assets/app.js && node --check assets/chart-navigation.js && node --check assets/strategy-engine.js && node --test tests-js/*.test.mjs",
    "parity": "node scripts/verify-parity.mjs",
    "parity:require-ready": "node scripts/verify-parity.mjs --require-ready",
    "check": "npm test"
  },
  "devDependencies": {
    "happy-dom": "20.11.0"
  }
}
