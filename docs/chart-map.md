# 차트·컨트롤 계약

화면은 `dashboard.json`·`history.json`에 이미 발행된 파생 결과만 선택한다. 브라우저는 회귀를 다시 적합하거나 임계값·사건 표본·백테스트를 최적화하지 않는다. 발행되지 않은 조합은 비활성화하고 마지막 정상값 또는 `unavailable`을 표시하며 임의의 시장 수치로 대체하지 않는다.

## 연구 트랙과 적용 범위

| 트랙 | 역할 | 입력·추정 | 기본 사용처 | 화면 표현 |
| --- | --- | --- | --- | --- |
| `robust` | 실전 신호 | 시장 규모 보정 수급 · 과거 데이터만 사용한 강건 회귀 | v2 현재값, 잔차, 사건, 기본 전략 | `실전 신호` 배지와 원형 keyline |
| `scaled` | 연구 기준선 | 시장 규모 보정 수급 · 과거 252일 OLS | 강건 회귀와 비교하는 기준선 | `OLS 기준선` 배지와 청록 keyline |
| `raw` | PDF 원문 근사 | 개인 순매수대금 절대값 · 과거 회귀 | 원문 주석 날짜와 방향 대조 | `PDF 원문 근사` 배지와 마름모 keyline |

`raw`는 원문 회귀 기간·이상점 제외·경계식이 공개되지 않았으므로 “원문 충실” 또는 “완전 재현”이라고 부르지 않는다. v1 payload에서는 `robust`가 없으므로 `scaled`로 안전하게 내려간다. 사건 연구는 `eventsByModel`이 있을 때 연구 트랙 선택을 따르고, 없으면 공개된 기본 사건 결과에 고정한다. 결론 배지는 사건 자산·연구 트랙·표본과 전략 프록시·규칙을 따로 표시한다. 전략은 별도의 신호·진입 규칙 선택을 사용한다.

## 차트 지도

| 영역 | 분석 질문 | 계열·형식 | 핵심 필드 | 벤치마크·불확실성 | 색 이외 구분 | 정확값·키보드 대안 |
| --- | --- | --- | --- | --- | --- | --- |
| 연구 결론 | 원문 재현·사건 일반화·거래 실용성 중 무엇이 지지되는가 | 3분할 근거 카드 | PDF 방향 일치, 사건 20일 평균·CI·초과수익, 전략 총수익·Sharpe·노출도 | 세 질문을 합쳐 하나의 판정으로 만들지 않음 | 번호·직접 텍스트·왼쪽 keyline | 사건·전략 표와 원문 스냅샷 |
| 신호 해석 브리지 | 절대 수급이 어떻게 상태로 변환되는가 | actual → expected → residual → percentile → state | 현재 입력, `alpha`, `beta`, `return1d`, `residual`, `percentile`, `state` | 회귀 예상값이 비교 기준 | 단계 번호·화살표·실제 수치·테두리 형태 | 한 문장 부호 해석과 현재값 카드 |
| KOSPI와 상태 | 실전 극단 상태와 실제 진입·보유구간이 가격 흐름의 어디에 있었나 | 단일 가격선 + 상태 관측 마커 + 실제 진입 삼각형 + 보유 배경 | `kospiClose`, `state`, `tradeEligible`, `position` | 가격 자체의 기준선 없음. 노출도 분모는 `long`·`cash` 산출 가능 거래일만 사용 | 공포 열린 원, 탐욕 마름모, 실제 진입 삼각형, 보유 배경 | 포인터·좌우/Home/End, 선택 기간 표, 최신 버튼, 기간별 롱·현금·산출불가 일수 |
| 수급 산점도 | 현재 수급이 가격-수급 관계에서 얼마나 벗어났고 어떤 상태 영역에 속하나 | relationship scatter + 회귀선 + 잔차 + 회귀선 평행 상태 영역 | `scatterByModel`, `scatterMetaByModel.<model>.stateBoundaries`, `return1d`, 수급, `role` | 최신 252일 학습 회귀선과 서버 발행 경험적 잔차 5·20·80·95% 경계. 경계가 없으면 영역을 숨김 | 학습 반투명 원, 현재 채운 원, 회귀선·경계 점선, 공포·탐욕 면 채움과 직접 라벨 | 가장 가까운 XY 관측점 hit-test, 날짜순 키보드, 당시 롤링 상태 표 |
| 잔차 백분위 | 기본 실전 상태가 고정 경계를 언제 통과했나 | 0–100 trend + threshold bands | `percentile`, `quality`, `date` | 5·20·50·80·95 고정선 | 선 형태·직접 눈금·결측 시 단절 | 날짜별 키보드, 최근 표, 최신 버튼 |
| 사건 검증 | 극단 상태 이후 평균효과와 불확실성이 어떠한가 | faceted dot & interval | 평균, `meanCi95`, 사건수, 선택적 benchmark/excess | 0%선, 공급된 benchmark, `excessMean`, `excessCi95` | 공포 원·탐욕 마름모·벤치마크 열린 사각형·CI cap | 정확값 표, nearest-point 키보드/포인터 |
| ETF 공통기간 | 같은 규칙에서 226490과 069500이 어떻게 다른가 | 2열 scorecard comparison | 총수익, CAGR, Sharpe, MDD, 노출도, 거래수, BH | 동일 공통기간·규칙·비용 | 직접 프록시 라벨과 동일 필드 순서 | 각 카드의 숫자와 백테스트 표 |
| 선택 전략 | 선택 프록시의 비용 후 경로와 낙폭은 어떤가 | 누적가치 trend + drawdown trend | sampled `equity`, 총수익, Sharpe, MDD, exposure, trades | 동일 진입·청산 타이밍 0bp, 같은 프록시 buy-and-hold; 총수익 우열은 직접 판정하지 않음 | 전략 실선·BH 점선 | 포인터·키보드·시작/끝 표·최신 버튼 |
| PDF 주석 사건 | 원문에서 표시한 11일이 독립 데이터에서 같은 방향인가 | exact lookup table + summary strip | `pdfReplica.annotatedEvents`, raw/robust/scaled percentile, forward returns | 완결된 후행수익만 표시 | 공포 원·탐욕 마름모·원문 근사 텍스트 | 표, 미완결 `—`, 원문 이미지 미복제 |
| 반도체 진단 | 환율·세션 정렬 후 MU 대비 하이닉스 상대 흐름은 어떤가 | 단일 trend | ratio/index/spread | 0 또는 100 중립선 | 중립 기준선·직접 단위 | 키보드·최근 표·최신 버튼 |

## 컨트롤과 상태 보존

- 연구 트랙: 실전 강건 회귀 / 규모보정 OLS 기준선 / 절대수급 PDF 원문 근사
- 사건: KOSPI / 226490 / 069500, 전체 / 20일 비중첩
- 전략: 프록시, 실전 강건 회귀 / OLS 기준선 / raw 원문 근사 / 이격도 강건성, 편도 0 / 5 / 10 / 20bp, 전체 / 공통기간
- KOSPI 가격 표시 기간: 기준일에서 달력 기준으로 계산한 1개월 / 3개월 / 6개월 / YTD / 1년 / 3년 / 전체, 또는 사용자가 지정한 시작일·종료일. 요청 경계가 휴장일이면 그 안의 실제 첫·마지막 거래일을 사용하고 화면에 표시 범위와 거래일 수를 공개한다. 520px 이하에서 처음 방문하면 1년으로 시작한다.
- 사용자 지정 기간은 시작·종료를 포함하며 역전, 공개 이력 밖, 거래일 0건을 거부한다. 8거래일 미만이면 표는 유지하고 추세 차트만 부족 안내를 표시한다. KOSPI 기간 선택은 잔차·회귀·사건·전략을 다시 계산하지 않는다.
- 잔차 표시 기간은 최신 756거래일(약 3년)로 고정한다.
- 모든 선택은 `fearngreed-controls-v2` localStorage와 URL query에 저장한다. “기본값 복원”과 “현재 화면 링크 복사”를 제공한다.
- v2 `history.seriesColumns` + `seriesRows`와 legacy `history.series`를 모두 수용한다.
- 표와 차트는 문서 전체가 아니라 자체 영역만 가로 스크롤하며, 모바일 시계열은 최신 구간으로 자동 정렬한다.

## 시각·접근성 원칙

Quant Research 공통 파랑을 기본 인터랙션으로, 청록을 기준선·비교 계열로, 주황을 원문 근사·탐욕 의미색으로 쓴다. 색만으로 상태를 구분하지 않고 원·마름모·사각형, 실선·점선, 직접 텍스트와 번호를 함께 사용한다. 모든 조작 버튼은 44px 내외의 터치 영역과 `focus-visible`을 유지하며 `prefers-reduced-motion`에서는 부드러운 스크롤·전환을 제거한다. 운영 상태 배지와 연구 신호 배지는 별도 DOM·문구·스타일로 표시한다.
